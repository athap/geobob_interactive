<% if params[:update_app_icons_drop] -%>
	jQuery('#app-icons-drop').attr('style', "<%= escape_javascript(app_background(@project, false).html_safe) %>");
	jQuery('#gpsrs-preview').attr('style', "<%= escape_javascript(app_background(@project, false).html_safe) %>");
<% elsif !@errors.blank? -%>
	jQuery('#message').html('<%= @errors %>');
<% end -%>
jQuery('.waiting').hide();
jQuery('.submit_wait').show();